{#
/**
 * @file
 * Default theme implementation for inline countdown elements.
 *
 * This template is used to render the inline countdown element.
 *
 * Variables:
 *  - attributes: HTML attributes for the containing element
 *  - countdown_date: Representation of the countdown date to pass to the frontend
 *  - countdown_settings: Additional parameters for configuring the countdown widget
 */
#}
<span {{ attributes }} data-countdown-inline="{{ countdown_date }}" data-countdown-settings="{{ countdown_settings|json_encode() }}"></span>
